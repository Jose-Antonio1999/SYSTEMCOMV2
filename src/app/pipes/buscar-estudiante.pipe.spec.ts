import { BuscarEstudiantePipe } from './buscar-estudiante.pipe';

describe('BuscarEstudiantePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarEstudiantePipe();
    expect(pipe).toBeTruthy();
  });
});
