import { BuscarComunicadosPipe } from './buscar-comunicados.pipe';

describe('BuscarComunicadosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarComunicadosPipe();
    expect(pipe).toBeTruthy();
  });
});
