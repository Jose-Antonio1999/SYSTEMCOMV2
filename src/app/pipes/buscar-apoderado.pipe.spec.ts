import { BuscarApoderadoPipe } from './buscar-apoderado.pipe';

describe('BuscarApoderadoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarApoderadoPipe();
    expect(pipe).toBeTruthy();
  });
});
