import { BuscarPersonalPipe } from './buscar-personal.pipe';

describe('BuscarPersonalPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPersonalPipe();
    expect(pipe).toBeTruthy();
  });
});
